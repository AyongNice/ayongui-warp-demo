---
nav:
  title: Components
---

# Button

Demo:

```tsx
import React from 'react';
// import { Message, Button } from 'ayongUI';

export default () => {
  const click = () => {
    Message.success({
      message: '成功',
    });
  };

  return <div>{/*<Button onClick={click}>info消息提示</Button>*/}</div>;
};
```
